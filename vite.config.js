import { defineConfig } from 'vite';

// https://vite.dev/config/
export default defineConfig({
  // This repo will be served at: https://signaajames.github.io/forms
  // So we set the base path to `/forms/`
  base: '/forms/',
});
